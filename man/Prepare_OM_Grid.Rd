% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_oms.R
\name{Prepare_OM_Grid}
\alias{Prepare_OM_Grid}
\title{Prepare SS3 run directories for an operating model grid}
\usage{
Prepare_OM_Grid(grid, ref_dir, growth_scenarios, M_scenarios)
}
\arguments{
\item{grid}{A `data.frame` as produced by [Build_OM_Grid()].}

\item{ref_dir}{Character. Reference SS3 model directory to copy from.}

\item{growth_scenarios, M_scenarios}{Named lists keyed by `grid$g_scen` /
`grid$m_scen` labels (e.g. `list(G_25 = list(Linf = ..., K = ..., t0 =
...), ...)` and `list(M_25 = 0.x, ...)`).}
}
\value{
Invisibly, `grid` (files are written as a side effect).
}
\description{
For each non-reference row of `grid`, copies the reference SS3 model
files into `run_dir` via [r4ss::copy_SS_inputs()] and overwrites growth
and natural mortality parameters using [Modify_Growth_M()]. The reference
cell (`is_ref == TRUE`) is left untouched, since it already exists at
`ref_dir`.
}
