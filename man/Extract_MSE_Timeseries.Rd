% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/performance_metrics.R
\name{Extract_MSE_Timeseries}
\alias{Extract_MSE_Timeseries}
\title{Tidy per-simulation, per-year SB/SBMSY, F/FMSY, and Yield time series}
\usage{
Extract_MSE_Timeseries(MSE, silent = TRUE)
}
\arguments{
\item{MSE}{An `mse` object (or list of `mse` objects, combined via
[MSEtool::CombineMSE()]).}

\item{silent}{Logical. Suppress the [MSEtool::CombineMSE()] summary
message when `MSE` is a `list`. Default `TRUE`.}
}
\value{
A tidy `data.frame` with columns `Sim`, `Stock`, `Year`, `Period`,
  `MP`, `Variable` (`'SB_SBMSY'`, `'F_FMSY'`, or `'Yield'`), `Value`.
}
\description{
Pulls the three time series needed for `Slick::Timeseries()`/
`Slick::Kobe()` `Value` arrays, and total `Yield` into one
long `data.frame`, covering both the `Historical` and `Projection` periods.
}
